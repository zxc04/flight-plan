@page
@model FlightPlan.WebSite.Pages.Flights.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Flight[0].DepartureAirport)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Flight[0].ArrivalAirport)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Flight[0].Plane)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Flight) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DepartureAirport.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ArrivalAirport.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plane.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
